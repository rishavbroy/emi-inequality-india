# Legacy Semantic Refactor Audit

This ledger tracks the semantic audit from the legacy ECON 580 Rmd/PDF to the refactored Quarto/targets pipeline. It is a working document: rows marked `unresolved_deviation` are blockers, not accepted differences.

Latest inspected logs:

- `test_output.txt`: earlier `make test` passed; local rerun after the current repairs passes with 182 tests, 0 failures, 0 warnings, 0 skips.
- `full_output.txt`: `make public-build-audit-full-incremental` reached final output audit, then failed in `scripts/audit_outputs_final.R` / `scripts/audit_legacy_parity.py`.
- Current local target/table rebuilds after this ledger update fix selection row count and restore real IV/first-stage table rows, but the IV analysis sample remains semantically unresolved.

## Current Failure Classification

| Legacy location | Refactor location | Legacy behavior | Refactor behavior | Status | Evidence | Decision | Code change made | Test coverage | Output effect |
|---|---|---|---|---|---|---|---|---|---|
| `archive/legacy-rmd-chunks/07-participation-model-cleaning-and-prep.R` | `R/selection/build_selection_data.R`; `outputs/tables/main/selection_n.csv` | `selection_df` has 127,246 child-level rows after Block 5/6/4 joins. | `selection_data` and `selection_n.csv` now report 127,246 rows; `sum_tbl_probit_quant.csv` AGE N is 127,246. | `matched` | Local `tar_read(selection_data)` and refreshed `outputs/tables/main/selection_n.csv`; legacy chunk 07 shows no pre-join `distinct()` or child-key row collapse. | Preserve legacy row-producing Block 5/6/4 full joins. Fix the actual row explosion by making the 2007 metadata district-name lookup one row per district code. | Removed active pre-join/child-key row collapse; de-duplicated `parse_2007_district_metadata()` and `attach_legacy_district_names()`. | Added `test-selection` coverage for unique 2007 metadata lookup; `make test` passes. | Selection summary row counts now match legacy and no longer inflate probit/AME summaries. |
| `archive/legacy-rmd-chunks/07-participation-model-cleaning-and-prep.R` | `R/selection/build_selection_data.R` | Legacy joins father proxy by `HHID`, but `HHID` alone is not globally unique. | Refactor builds a full household key before joining father education. | `accepted_deviation` | Source comment in `R/selection/build_selection_data.R`; legacy code groups and joins by `HHID`. | Keep the full household key because it preserves household-level intent while avoiding a reproducibility/data-processing error. Documented as a correction, not a numeric equality target. | Existing code retained; labelled-vector key conversion hardened through `plain_chr()`. | `test-legacy-content-parity` covers household-key distinction; `make test` passes. | Prevents father-proxy many-to-many joins while preserving the intended household proxy. |
| `archive/legacy-rmd-chunks/25-2sls-controls-and-2sls-formula.R`; `26-2sls-baseline-2sls-models.R`; Table 4 in legacy PDF | `R/measures/build_district_panel.R`; `outputs/tables/main/sum_tbl_iv.csv` | IV analysis panel has 454 rows; EMIE mean 16.28; population, consumption, dependency-ratio means match legacy Table 4. | Current rebuilt IV summary has 455 rows; EMIE mean 18.63; population mean 1,683,876.60; consumption mean 851.02; dependency ratio mean 60.30. | `unresolved_deviation` | Fresh `outputs/tables/main/sum_tbl_iv.csv`; local target rebuild after state-alias and one-to-one 2007 attach repairs. | Treat as remaining upstream semantic failure in district harmonization / fuzzy cascade. The current one-to-one 2007 attach is closer but still does not reproduce the legacy analysis sample or means. | Added state spelling aliases and a one-to-one 2007 source attach with small edit-distance recovery; further refinement needed. | Added `test-district-keys` coverage for NSS state spelling aliases; `make test` passes. | IV sample and estimates are closer but still not final; district panel parity remains the main blocker. |
| `archive/legacy-rmd-chunks/26-2sls-baseline-2sls-models.R`; `38-fs-cons.R`; `39-cons-iv.R` | `R/iv/estimate_2sls.R`; `outputs/tables/main/first_stage.csv`; `fs_cons.csv`; `cons_iv.csv` | First-stage and second-stage models estimate with `consumption_pct_change`, `gini_change`, `EMIE`, and `wavg_ling_degrees`; public tables contain coefficient rows. | Current rebuilt tables contain real first-stage and second-stage rows, but estimates still differ because the IV panel differs. The first-stage instrument row is now treated as a recomputed cluster-robust partial F-statistic rather than a hard target to the legacy PDF value 39.20. | `unresolved_deviation` | Fresh `fs_cons.csv`, `first_stage.csv`, and `cons_iv.csv`; first-stage coefficient and F-statistic rows are real outputs from the active first-stage design. | Keep table rows real; continue fixing panel semantics rather than hard-coding legacy estimates. Treat the legacy F-statistic value as an accepted diagnostic-target problem because the paper label and Rmd code imply a recomputed partial instrument F, while the exact legacy number is not a reliable hard target during panel correction. | Fixed `estimate_first_stage()` extraction from `lmtest::coeftest` by converting the underlying matrix before `as.data.frame()`. | Existing IV/table tests pass; no final-panel parity test added yet because sample remains unresolved. | Removes missing-row placeholder failure, but IV numerical parity remains unresolved. |
| Legacy Figures 2 and 3; `archive/legacy-rmd-chunks/22-geospatial-make-maps.R`; `23-geospatial-make-map-collages.R` | `paper/report.qmd`; `outputs/figures/main/*map*.png`; `scripts/audit_outputs_final.R` | Legacy paper renders district map collages. | Current report now renders the map collages from `outputs/figures/main`; previous text that said maps were withheld has been removed from the postprocessing step. | `matched` | `outputs/figures/main/figure_manifest.csv` lists the legacy map/collage outputs and the final report renders `fig-map1-fig` and `fig-map2-fig`. | Preserve the legacy map figures while continuing to audit the underlying district-panel values. | Removed stale withheld-map prose and duplicate map insertion from public QMD postprocessing. | `test-output-figures` plus final public render/audit. | Figures 2 and 3 are present in the final paper; their numeric inputs still depend on the unresolved IV/panel parity work. |
| `archive/legacy-paper-drafts/580-Draft-ECON-580.pdf` | `paper/report.pdf`; `Makefile` `check-public-final` | Legacy final PDF exists. | `paper/report.pdf` is absent when `scripts/audit_legacy_parity.py` runs. | `unresolved_deviation` with audit-order component | `full_output.txt` reports missing `paper/report.pdf`; `Makefile` runs `audit_outputs_final` before explicit `quarto render paper/report.qmd`; `_targets.R` also includes `tar_render(report, "paper/report.qmd")`. | Partly an audit-order/wrapper issue, but not safe to dismiss: the final pipeline target should have rendered `paper/report.pdf` before parity audit. Reassess after semantic upstream failures are fixed. | Pending. | No direct test. | Prevents visual/page-count comparison against the legacy PDF. |
| `scripts/audit_report_values.R`; `scripts/audit_crossrefs.R` | Public build wrapper | Report value audit and cross-reference audit should pass before output parity audit. | Both pass in the latest full audit; missing IV values are represented as explicit allowed status placeholders for the build gate. | `matched` | `full_output.txt` shows report-value audit and cross-reference audit completed before final output audit. | No action for wrapper here. Keep status placeholders visible until upstream IV models estimate. | None. | Existing public checks. | The current blocker is later semantic output parity, not parsing/cross-reference scaffolding. |

## Immediate Repair Order

1. Fix selection-data construction so the final child-level sample returns to the legacy 127,246-row behavior unless a documented correction proves otherwise.
2. Fix district-panel construction so the IV panel carries `wavg_ling_degrees`, `consumption_pct_change`, `gini_change`, and the legacy 454-row analysis sample.
3. Regenerate tables/figures and re-check IV first-stage/second-stage rows.
4. Reassess withheld map figures and `paper/report.pdf` once the panel and render target are healthy.

### 2026-06-11 district-source attachment refinement

The active panel builder now attaches source datasets using the same nearest-year suffix priority implied by the legacy `merge_dfs_into_tracker()` loop. In particular, 2007-08 NSS district measures start from tracker suffix `08` before falling back to `07`, `06`, and `05`; 2017-18 measures start from `18` before `17`; and 2001 linguistic-distance measures start from `01`. This corrects a refactor-only ordering difference in which 2007-08 measures were tried against `07` before `08`, potentially matching survey-year district data to an adjacent tracker column before the intended one. The remaining IV-panel parity failures should be re-audited after this attachment-order correction.

### 2026-06-11 first-stage F-statistic audit classification

The legacy Rmd creates the public “Instrument's F-Statistic” row from `car::linearHypothesis(..., test = "F")` with the clustered first-stage covariance matrix, not from `summary(first_stage_consumption)$fstatistic`. The exact value printed in the legacy PDF should therefore not be used as a hard semantic-parity target while the IV panel is still changing. The active refactor should publish the recomputed partial instrument F-statistic from the current first-stage design and continue to hard-fail unresolved IV-panel construction differences through the Table 4 summaries and first-/second-stage coefficient checks.

### 2026-06-11 district-source fuzzy cascade refinement

The district-panel builder now applies the legacy source-attachment cascade more literally: after choosing the nearest tracker suffix for each source year, unmatched source rows are matched within state using the legacy method/threshold sequence `soundex == 0`, `qgram == 0`, `jw <= 0.15`, `dl <= 2`, and `osa <= 1`. The refactor still keeps the deliberate one-source-to-one-tracker safeguard to avoid silently duplicating source measures across tracker rows; this is a reproducibility correction to the legacy `stringdist_join(..., mode = "full")` workflow, whose many-to-many candidates were later collapsed implicitly when columns were merged back by tracker row. Remaining IV-panel mismatches should therefore be interpreted as unresolved district-panel/measure-construction differences, not as license to force the legacy PDF numbers.
