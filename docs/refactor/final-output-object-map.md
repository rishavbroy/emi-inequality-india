# Final output object map

This map records the current source of every full-report figure and table cross-reference that final mode must resolve. It is a technical tracking file for the semantic refactor; it does not add new paper prose.

| referenced object | source function or legacy chunk | output path | target name | final report label | status | notes |
|---|---|---|---|---|---|---|
| `fig-ILO-fig` | `make_ilo_trends_figure()`; legacy chunk `31-ilo-fig` | `outputs/figures/main/fig_ilo_trends.png` | `figures`, `figure_files` | `fig-ILO-fig` | draft implemented; final label wired | The report now includes a Quarto-labeled image object; the file is composed from archived ILO figure assets. |
| `fig-map1-fig` | `make_map_collages()`; legacy chunks `22-geospatial-make-maps`, `23-geospatial-make-map-collages`, `35-map1-fig` | `outputs/figures/main/collage_main_maps.{png,pdf}` | `figures`, `figure_files` | `fig-map1-fig` | final blocked until geometry coverage is validated | Final mode now fails unless the district panel is an `sf` object with at least 75% non-empty geometry coverage; draft diagnostics are kept under `outputs/diagnostics/figures/`. |
| `fig-map2-fig` | `make_map_collages()`; legacy chunks `22-geospatial-make-maps`, `23-geospatial-make-map-collages`, `37-map2-fig` | `outputs/figures/main/collage_iv_region_maps.{png,pdf}` | `figures`, `figure_files` | `fig-map2-fig` | final blocked until geometry coverage is validated | Uses real `sf` maps when the geometry join is validated; no final diagnostic substitute is allowed. |
| `fig-districtcarveoutsshifts-fig` | legacy chunks `06-match-districts-construct-district-tracking-dfs`, `40-districtcarveoutsshifts-fig`; `save_district_carveouts_shifts()` | `outputs/figures/main/district_carveouts_shifts.{png,pdf}` | `figures`, `figure_files` | `fig-districtcarveoutsshifts-fig` | implemented from Kumar-Somanathan carveout source | Recreates the legacy histogram of `pct_91in01`; source-coverage charts are no longer written to this final figure path. |
| `tbl-sum-tbl-iv` | `make_iv_summary_table()`; legacy chunk `36-sum-tbl-iv` | `outputs/tables/main/sum_tbl_iv.{csv,tex}` | `tables`, `table_files` | `tbl-sum-tbl-iv` | draft implemented; final requires full model variables | The report chunk includes generated TeX in LaTeX output and falls back to CSV for HTML. |
| `tbl-sum-tbl-probit-quant` | `make_selection_summary_numeric_table()`; legacy chunk `32-sum-tbl-probit-quant` | `outputs/tables/main/sum_tbl_probit_quant.csv` | `tables`, `table_files` | `tbl-sum-tbl-probit-quant` | draft implemented | The report now includes a Quarto-labeled table chunk reading this CSV. |
| `tbl-sum-tbl-probit-cat` | `make_selection_summary_categorical_table()`; legacy chunk `33-sum-tbl-probit-cat` | `outputs/tables/main/sum_tbl_probit_cat.csv` | `tables`, `table_files` | `tbl-sum-tbl-probit-cat` | draft implemented | The report now includes a Quarto-labeled table chunk reading this CSV. |
| `tbl-probit-mfx` | `make_probit_ame_table()`; legacy chunk `34-probit-mfx` | `outputs/tables/main/probit_mfx.csv` | `ame_results`, `tables`, `table_files` | `tbl-probit-mfx` | draft status output; final requires full AME | Draft mode preserves the explicit AME status row; final mode fails if AME output remains unavailable. |
| `tbl-fs-cons` | `make_first_stage_table()`; legacy chunk `38-fs-cons` | `outputs/tables/main/fs_cons.csv` | `first_stage_tests`, `tables`, `table_files` | `tbl-fs-cons` | draft status output; final requires first-stage model | Draft mode preserves explicit first-stage status rows; final mode fails if first-stage output remains unavailable. |
| `tbl-cons-iv` | `make_second_stage_table()`; legacy chunk `39-cons-iv` | `outputs/tables/main/cons_iv.csv` | `iv_models`, `tables`, `table_files` | `tbl-cons-iv` | draft status output; final requires second-stage model | Draft mode preserves explicit second-stage status rows; final mode fails if IV models remain unavailable. |
