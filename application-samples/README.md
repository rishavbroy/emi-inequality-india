# Application samples

This folder contains cover notes, sample specifications, and generated PDFs for pre-doc and research-assistant applications. Final PDFs in `application-samples/output/` are tracked so reviewers can read them directly from GitHub.

The writing samples are generated from marked excerpts in `paper/report.qmd`. The coding samples are generated from marked code excerpts in `R/` files.
