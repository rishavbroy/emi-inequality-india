---
title: "AME Benchmark"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(bench_ame_methods)
```

This notebook should render from target outputs, not manually copied numbers.
