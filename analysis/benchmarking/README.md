# Benchmarking notebooks

These notebooks are lightweight readers for opt-in benchmark target outputs. They should render from `targets` artifacts or files under `outputs/benchmarking/`, not from manually copied numbers.
