---
title: "Spatial IV Experimental Models"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(spatial_iv_experimental)
```

This notebook should render from target outputs, not manually copied numbers.
