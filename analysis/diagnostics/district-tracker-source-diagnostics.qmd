---
title: "District Tracker Source Diagnostics"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(diag_ext_district_tracker_sources)
```

This notebook should render from target outputs, not manually copied numbers.
