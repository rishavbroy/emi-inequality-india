---
title: "AME Benchmark"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(diag_ame_benchmark)
```

This notebook should render from target outputs, not manually copied numbers.
