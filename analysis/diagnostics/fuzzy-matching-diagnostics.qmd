---
title: "Fuzzy Matching Diagnostics"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(diag_ext_fuzzy_matching)
```

This notebook should render from target outputs, not manually copied numbers.
