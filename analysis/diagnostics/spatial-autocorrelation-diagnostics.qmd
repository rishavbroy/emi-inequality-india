---
title: "Spatial Autocorrelation Diagnostics"
format:
  html: default
  pdf: default
---

```{r}
library(targets)
# x <- tar_read(diag_spatial_autocorrelation)
```

This notebook should render from target outputs, not manually copied numbers.
