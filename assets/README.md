# Poster assets

`uw-logo-horizontal-full-color-print.pdf` is the official UW–Madison horizontal wordmark used by the poster.

`repo-qr.svg` links to the repository landing page used on the poster.

Both files remain authoritative source assets under `assets/`. Quarto resolves these named resources for the Typst render.
