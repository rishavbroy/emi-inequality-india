# Publication assets

This directory contains shared publication assets that are not generated by the analysis. The current tracked files support poster/publication presentation, including the repository QR graphic and university logo.

Analysis-generated figures belong under `outputs/figures/`; poster-specific generated material belongs with the poster output. Add a file here only when more than one publication surface needs the same static asset or when keeping it at repository level avoids duplication.
